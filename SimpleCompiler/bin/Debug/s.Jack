function int f1( int a, int b){
	var int r;
	var int c;
	while( (> b 0 ) ){
		let r = (- a (* b (/ a b)));
		let a = (> b 1);
		let b = f2(x);
		let c = f3(+ a 1), (- b (+a 1));
	}
	return a;
}
